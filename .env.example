# =============================================================================
# ShortsRanker Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# Required variables are marked with [REQUIRED]
# Optional variables have sensible defaults

# =============================================================================
# TTS (Text-to-Speech) Configuration
# =============================================================================
# Choose your TTS service. Options: mock, huggingface, elevenlabs, coqui
# Default: mock (generates silent audio for testing)
TTS_SERVICE=mock

# Hugging Face API Key (for free TTS)
# Get your key at: https://huggingface.co/settings/tokens
# [OPTIONAL] Required only if TTS_SERVICE=huggingface or for LLM script polishing
HUGGINGFACE_API_KEY=

# Hugging Face TTS Model (optional, defaults to facebook/mms-tts-eng)
# Other options: espnet/kan-bayashi_ljspeech_vits, microsoft/speecht5_tts
HUGGINGFACE_TTS_MODEL=facebook/mms-tts-eng

# ElevenLabs API Key (for premium TTS)
# Get your key at: https://elevenlabs.io/
# [OPTIONAL] Required only if TTS_SERVICE=elevenlabs
ELEVENLABS_API_KEY=

# ElevenLabs Voice ID (optional, defaults to Adam voice)
ELEVENLABS_VOICE_ID=pNInz6obpgDQGcFmaJgB

# Enable local Coqui TTS (requires Python + pip install TTS)
# [OPTIONAL] Set to true only if you have Coqui TTS installed locally
USE_COQUI_TTS=false

# =============================================================================
# Server Requirements
# =============================================================================
# FFmpeg is REQUIRED for video processing
# Install via: apt-get install ffmpeg (Ubuntu/Debian)
#              brew install ffmpeg (macOS)
#              choco install ffmpeg (Windows)

# =============================================================================
# Processing Limits (informational)
# =============================================================================
# - Maximum 5 videos per compilation
# - Each video should be under 60 seconds for best results
# - Total processing time depends on video lengths and server resources
# - Recommended server: 2+ CPU cores, 4GB+ RAM for smooth processing
